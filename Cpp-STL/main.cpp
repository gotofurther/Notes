#include <bits/stdc++.h>
using namespace std;

int dic[25];

int main(){
	vector<char> str;
	char a;
	while(scanf("%c", &a), str.push_back(a), a != '\n');
	for(int i = 0; i < str.size(); i++){
		dic[sttr[i] - 'a']++;
	}
	for(int i = 0; i < str.size(); i++){
		
	}
}
